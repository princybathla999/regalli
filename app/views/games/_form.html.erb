<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :opponent %><br>
    <%= f.select :opponent,options_for_select(@other_user.collect{|x| [x.email,x.id]}) %>

  </div>
  <div class="field">
    <%= f.label :my_score %><br>
    <%= f.text_field :my_score %>
  </div>
  <div class="field">
    <%= f.label :opponent_score %><br>
    <%= f.text_field :opponent_score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
